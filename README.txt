Development

Install components:

    npm install
    bower install

Start the server in one terminal:

    nodemon -w server bin/www

Start the grunt tasks in another terminal:

    grunt dev

Production:

Invoke grunt to build minifies and uglified javascript:

    grunt production
